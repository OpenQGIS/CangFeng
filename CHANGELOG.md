# Changelog · 藏锋录 (CangFeng)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范，版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。

---

## [1.2.4] - 2026-09-25

### 新增 (Added)
- **多画幅比例典藏海报引擎 (Multi-Aspect Ratio Artwork Poster Engine)**：
  - 弹窗标题重塑为比例后置国际化格式（如 `Artwork Poster 3:4` / `典藏画卷海报 3:4`）；
  - 提供 5 种主流画幅快捷切换选单：`3:4` 竖版典藏、`4:3` 横版画廊、`16:9` 宽屏全景、`9:16` 移动全屏、`9:21` 极客长幅；
  - 横版比例（`4:3`、`16:9`）自适应切换为瑞士包豪斯双栏画册排版，左侧巨幅无损展台铺展，右侧集成作品案名、分类、工造题记、色板圆角胶囊与专属二维码，彻底解决横幅地图在竖版海报中上下留黑严重的问题；
  - 动态自适应导出文件名（中英双语与比例前缀自适应）。
- **海报全语境国际化与西文折行优化 (Poster i18n & Word-Aware Typography)**：
  - 海报内部画卷题名、分类/工造匠师、Slogan、二维码导引、底部版权全面匹配当前语言环境（中/英/日/韩四语自适应）；
  - 引入 Word-Aware 单词换行算法，彻底消除英文单词被中间截断拆行的排版缺陷；
  - 二维码导引文案采用右对齐反向伸展，杜绝多国语言长短不一与二维码卡片重叠。

### 优化 (Optimized)
- **根除底栏悬停双重提示框冲突 (Eliminate Toolbar Dual Tooltip Collision)**：
  - 彻底剥离底部悬浮工具栏所有功能按钮在 HTML 静态标签中硬编码的原生 `title` 属性，升级为 `aria-label` 配合 `data-custom-tip`；
  - 优化国际化多语言翻译引擎（`i18n.js`），针对工具栏及其子项定向更新 `data-custom-tip`，禁止动态写回原生 `title`，从根源切断操作系统/浏览器老式矩形原生黑白提示框的触发源；
  - 工具栏悬浮提示事件（`showTip`）增加防御性拦截，确保多端仅且只展示专属打造的高质感毛玻璃微胶囊气泡（`#dockTooltip`，含高反差键位徽标），消灭双气泡重叠与视觉跳动。
- **底部悬浮工具栏图标与数值极简实体化 (Toolbar Pure Vector Solid Polish & Shadow Removal)**：
  - 彻底移除底部悬浮工具栏各功能图标（全貌、旋转、视口全屏、档案、分享、翻卷箭头）与缩放比例数值上的历史遗留 `drop-shadow` 与 `text-shadow` 硬黑投影；
  - 彻底消除因图标几何密度差异（封闭几何框投影重叠黑浓 vs 细线折线单薄无影）导致的主观粗细不一与视觉重量失衡，抹平缩放胶囊下拉箭头 `.chevron-icon` 的阴影断层漏洞；
  - 依托底栏已具备的墨玉遮光内核（68%~78% 深度防吞蚀阻尼）提供扎实底座，暗色模式下使纯白冷光图标（`#f8fafc`）与缩放数字恢复钟表刻度级的极清锐利矢量质感，杜绝白底图上的毛刺与重影，呈现克制利落的现代高级工造美学。
- **恢复底栏气泡弹出与层叠上下文提权 (Restore Toolbar Popovers & Z-Index Elevation)**：
  - 彻底移除底部悬浮工具栏因伪元素反光膜误设的 `overflow: hidden`，恢复默认 `overflow: visible`，彻底解决移动端与桌面端点击“分享画卷”与“比例控制器”菜单被父容器物理裁剪消失的问题；
  - 联动升级提权规则：当分享气泡打开时（`:has(#sharePopover.open)`），与比例气泡同步将工具栏提权至 `z-index: 35`，确保多端菜单压制展开，交互流畅无阻。
- **画廊卡片底栏视觉降噪（彻底移除冗余【深览】角标） (Card Meta Noise Reduction & Anti-AI Polish)**：
  - 彻底清除瀑布流每幅画卷右下角重复的【深览】文本与放大镜图标，消除全屏机械重复的冗余噪声；
  - 依托整卡可点（`role="button"`）、指针手型及 `scale(1.06)` 悬停反馈自然建立交互意图，达成顶级独立艺术画廊的克制与极简美学；
  - 彻底释放卡片底栏横向空间，使多类目属性标签从容铺展，消除文字挤压与截断。
- **右上角沉浸式视口控制栏与看图自动折叠 (Immersive Floating Actions & Auto-Folding)**：
  - 低频配置项（语言切换与主题切换）与高频退出（关闭）视觉解耦；
  - 闲置看图 2.8 秒后自动进入沉浸式浅灰折叠状态（22% 透明度），交互（鼠标微移、平移、缩放）毫秒级唤醒还原，创造极致纯粹看图体验。
- **底部分享气泡菜单紧凑收窄与留白优化 (Share Popover Layout Refinement)**：
  - 彻底移除移动端下将分享气泡强行撑大至 280px 的冗余规则，收窄为 218px~220px 极致契合内容尺寸；
  - 消除“分享网址”、“分享二维码”、“海报分享”卡片右侧多达 80px+ 的空洞死黑留白，建立匀称呼吸空间。
- **二维码分享弹窗全端视觉重塑与专属头像徽标 (QR Code Modal Avatar Stamp & Spacing Optimization)**：
  - 二维码正中心正式接入地图左上角同款经典 OpenQGIS 专属头像（`assets/avatar.png`，绿底罗盘眼图腾），配合 15% 纠错安全衬垫，极具品牌专属识别度与开源极客质感；
  - 彻底治愈左右两侧死白问题：收缩二维码内置静区至 `margin: 1`，并将容器 padding 精紧至 `7px`，点阵自然舒展铺满，消灭空洞多余白边；
  - 移除机械重复的副标题 `1:1 4K Deep Zoom 深览`，使界面聚焦于分类胶囊、物理超高分辨率标签与作品案名，纵向布局更紧凑透气；
  - 保存功能同步升级：点击“保存二维码”时动态绘制带有该专属头像徽标、题跋与分辨率参数的精美文创便签图；
  - 引入防缓存体系（`Cache-Control: no-cache`）与全量静态资源 `v20260925_v20` 版本戳，杜绝移动端强缓存失效。
- **底部悬浮工具栏重塑为双轨制自适应液态玻璃 (Adaptive Optical Liquid Glass Architecture)**：
  - 针对桌面端与移动端的输入方式、屏幕尺寸与渲染算力差距，落地双轨制渐进增强液态玻璃架构；
  - **浅色主题比例数字高精锐化**：彻底剥离浅色模式下残留的黑色文字阴影（`text-shadow: none !important`），字色升级为沉稳高反差的深墨绿 `#244904`，消除重影与边缘毛刺发虚，还原极致锐利的矢量字形；
  - **通用核壳阻尼与白底抗侵蚀**：确立“边缘水润高透、核心墨玉压光”核壳模型，底座内核加固至 65%~78% 墨玉深色基底，并将环境反光膜限制于顶部水滴弧，彻底杜绝刺眼亮白宣纸底图（如《成都剪纸》）穿透底座造成的全局反白发灰；
  - **纯白图标与微米级暗影**：深色模式下图标全面提权至高纯冷白 `#f8fafc`，矢量描边加固至 `2.15px`，并注入全天候自适应防侵蚀微阴影 `drop-shadow(0 1px 2px rgba(0,0,0,0.75))`，在任何极黑/极白/杂色底图上均恒定维持 WCAG 高对比度；
  - **环境反射与棱镜色散**：胶囊外圈叠加 `0 0 0 1px / 2px` 红蓝双轨棱镜微色散环，让边缘反光根据底层地图实时融入（海洋蓝、沙漠金、雪山白），彻底告别死白机械线条；
  - **移动端与性能保护**：引入单条高反差 `1.5px` 水滴反射顶弧与触控按下（`:active`）果冻弹性微挤压反馈，并轻量化模糊半径，确保移动端巨幅地图缩放 120fps/60fps 满帧如丝。



### 修复 (Fixed)
- **深览视口右上角主题切换双重触发失效修复 (Theme Toggle Double-Trigger Fix)**：
  - 根除 `app.js` 与 `theme.js` 对 `#toolToggleTheme` 重复注册点击监听导致同一帧内“深色➔浅色➔深色”瞬间抵消的缺陷；
  - 在 `theme.js` 中引入 `dataset.themeBound` 防重锁与 `document.readyState` 兼容机制，保证深浅色模式切换绝对可靠生效。

---

## [1.2.3] - 2026-09-25

### 修复 (Fixed)
- **首页右上角全局按钮幽灵遮罩穿透修复 (Hero Controls Hit-Testing Fix)**：
  - 彻底根除未激活全屏阅览弹窗（`.viewer-modal`）其内部子元素 `pointer-events: auto` 穿透覆盖父级导致首页语言下拉与主题切换被不可见全屏层阻断拦截的严重缺陷；通过为弹窗施加 `visibility: hidden` 与 `:not(.open) * { pointer-events: none !important; }`，保障首页交互 100% 灵敏响应。
- **深览图名底图自适应反色与白雾排版净化 (Viewer Typography Contrast & Anti-AI Polish)**：
  - 彻底剔除原浅色主题下强行叠加的 20px 扩散漫反射白雾阴影（`text-shadow` 脏块），根据画卷主体底色亮度动态判定与反色：浅色底图自适应深墨色字（`text-shadow: none`，还原原版纯粹利落质感），深色底图反色为纯白字，并同步联动 QMapFlow 精度徽标明暗卡片，恢复精致字号（0.95rem / 600）。

### 优化 (Optimized)
- **底栏比例控制器二合一内嵌输入与锚点指示 (Zoom Inline Input & Anchor Caret)**：
  - 将底部比例胶囊重塑为内嵌数值输入框与展开箭头二合一（`[ 100 ] % ▾`），支持在底栏直接键入缩放百分比或点击预设展开；
  - 比例弹出面板增加精准指向底栏胶囊的居中三角形小锚点；预设列表精简展示纯数值百分比（50%、100%、200% 等），剔除冗余修饰。
- **视口图标重构 (Iconography Refinement)**：
  - 整图复位（`toolReset`）图标重构为国际标准 `fit_screen` 四角视口框线图标，消除与旋转图标的视觉混淆；
  - 顺时针旋转（`toolRotate`）图标优化为标准圆润顺时针圆弧箭头，与逆时针版弧度对称优美。
- **鹰眼图底边透明留白消除与移动端抽屉手势优化 (Minimap & Mobile Optimizations)**：
  - 统一 `.viewer-navigator-panel` 边框与阴影，重构尺寸计算消除底部透明模块；
  - 移动端浮动抽屉高度调优为 `48vh`，增加画布平移/捏合手势自动折叠防遮挡；
  - 移动端分享下拉菜单居中对齐展开，展开时自动隐藏鹰眼图避免界面拥挤。

---

## [1.2.2] - 2026-09-25

### 优化 (Optimized)
- **瀑布流卡片微浮动效 (Waterfall Card Refinement)**：
  - 采用平缓上浮（Scheme A）：底边预留 20px 间距，悬停位移从 -6px 优化至 -3px，底栏元数据与二级标签平滑展开，彻底解决底端紧贴视口的不适感。
- **深览视口左上角通透烙印 (Viewer Top-Left Brand Imprint)**：
  - 采纳极简通透烙印方案（Scheme 3）：移除 42px 黑色 OpenQGIS 头像与白色卡片包裹背景，采用半透明浮动文本烙印与柔和阴影，图名与 QMapFlow 精度工作流标识清晰醒目且不遮挡地图图面细节。
- **右上角全局操作胶囊多语言切换适配 (Viewer Language Selector)**：
  - 移除原固定 36px 圆形限制，设为自适应宽度的药丸胶囊（min-width: 52px），彻底解决“中/EN/JA/KO”文字与下拉箭头错位或溢出的缺陷。
- **全屏聚焦蒙版与分享按钮视效 (Share Focus & Dimming)**：
  - 点击“分享”弹出二级下拉菜单时，地图底层自动铺开轻柔暗场对焦蒙版（rgba(0,0,0,0.5) + 2.5px 景深微模糊），使分享菜单成为视觉绝对焦点；点击蒙版平滑收起。
- **分享文案与光标操作反馈强化 (Share Options & Cursor Toast)**：
  - 精简海报分享文案为【海报分享】；点击“复制网页链接”时，鼠标指针跟随弹出半透明悬浮反馈胶囊「✓ 网址已复制」，并在 1.8 秒后淡出移除。

---

## [1.2.1] - 2026-09-23

### 修复 (Fixed)
- **移动端与国内公网大图深览加载失败缺陷修复 (Mobile & Mainland Network Tile Delivery Fix)**：
  - **同源瓦片金字塔内置**：同步内置 `CangFengCore` 全量 5 幅作品 8.04MB WebP 瓦片金字塔目录（`tiles/`），解开 `.gitignore` 限制，使 GitHub Pages 支持同源直接分发，彻底摆脱国内手机蜂窝网络及无代理环境下外部 Cloudflare Workers 免费域名（`*.workers.dev`）被 SNI 阻断导致大图视口黑屏的问题。
  - **默认直连策略优化**：在 `assets/config.js` 中将 `assetBaseUrl` 默认置空，优先采用本站同源相对路径 `tiles/` 极速加载，免翻墙、免跨域、零网络延迟。
  - **首帧即时渲染保障 (`placeholderImage`)**：为 OpenSeadragon 视口配置 `placeholderImage: item.thumb` 与 `immediateRender: true`，点开大图 0ms 呈现画作预览，彻底消除等待空白期。
  - **多级网络容灾降级引擎**：若配置了远程 CDN 且瓦片连续加载失败，系统自动无缝降级切换至本地同源 `tiles/` 或已加载的预览全图，保障在任何弱网或断网离线环境下均能顺畅观赏大图。

### 优化 (Optimized)
- **移动端竖屏与触控交互适配 (Mobile UI/UX Refinement)**：
  - 针对手机小屏（`max-width: 768px`）优化顶栏布局，收拢右侧按钮间距，隐藏冗余退出键，确保作品名称完整居中呈现；
  - 手机端隐藏左右侧边翻页大箭头（`.viewer-edge-nav`），彻底消除双指捏合缩放（Pinch-to-zoom）与单指拖拽平移时的误触与视觉遮挡；
  - 底部浮动工具栏紧凑化重构，适配小屏边距与触控尺寸；
  - 移动端浮动信息面板自适应转为底部抽屉式卡片（Bottom Sheet），浏览档案更贴合移动端操作直觉。

---

## [1.2.0] - 2026-09-21

### 新增 (Added)
- **浮动信息提示悬窗 (Floating Metadata Window)**：
  - 将原本右侧固定的推拉抽屉重构为高质感磨砂玻璃浮动悬窗（Glassmorphic Floating Panel）。
  - **自由拖拽移动**：支持按住悬窗顶部手柄（带三道横线 Grip 视觉标记）拖拽至屏幕任意位置。
  - **8 向拉伸调整尺寸 (8-directional Resize)**：支持对四角（NW, NE, SW, SE）及四边（N, S, W, E）进行尺寸拖动拉伸，光标自适应变化，右下角带有斜纹指示标记。
  - **视口边界防护 (Viewport Clamp)**：拖拽与拉伸均内置 16px 视口吸附保护，避免窗口被拖出屏幕外；浏览器窗口大小改变时自动回弹至安全可见区域。
  - **快捷键呼出**：支持键盘 `Tab` 键及 `I` 键一键弹出 / 收起信息悬窗，带来沉浸式观览体验。
  - **自适应动态透光度机制 (Adaptive Opacity Interaction)**：
    - 鼠标在悬浮窗上悬停时，呈现清晰易读的 **80% 不透明度**（`opacity: 0.8`）；
    - 鼠标在地图上进行滚轮或手势缩放时，悬窗瞬间降为 **30% 不透明度**（`opacity: 0.3`），实现极致通透的底层空间纹理观察体验，停止缩放后平滑过渡恢复。

### 优化 (Optimized)
- **阅览视窗顶栏视觉降噪 (Header De-cluttering)**：
  - 移除左上角冗余的 `CANGFENG 4K` 徽标标签与尺寸像素副标题（已在 Tab 呼出的信息悬窗中完整呈现），仅保留退出按钮与单行垂直居中的纯粹作品名，彻底消除视觉杂质与顶栏切边挤压感。

### 修复 (Fixed)
- **描述文本横向溢出问题 (Text Overflow Fix)**：
  - 修复 `.drawer-content` 默认 `overflow-y: auto` 导致 `overflow-x` 隐式计算为 auto 引起的长文本溢出问题，显式声明 `overflow-x: hidden`。
  - 为 `.meta-desc` 与 `.meta-desc-text` 补充 `overflow-wrap: break-word` 与 `max-width: 100%`，确保工造题记与解析度说明在任意悬窗宽度下自适应折行。
- **主调色板萃取无结果缺陷修复 (Color Palette Extraction Fix)**：
  - 修复 `extractDominantColors` 异步图像加载时遗漏给 `img.src` 赋值导致 `onload` 永远无法触发、面板持续卡在“提取中...”的致命缺陷。
  - 引入 DOM 已加载卡片缩略图优先内存复用机制（0ms 秒级响应，无需发起二次网络请求）。
  - 增强颜色欧氏距离色差聚类去重算法，并内置高精度专属工造主色谱优雅兜底，杜绝白屏或提取失败。

---

## [1.1.0] - 2026-09-21

### 新增 (Added)
- **双层隐形数字盲水印取证引擎 (Stealth Watermark Engine)**：
  - 集成前端自检网格与取证系统，支持快捷键 `Alt+W` 或 `Shift+W` 触发取证 HUD 探针，显影隐藏在画幅纹理中的版权追踪签名。
  - 引入微透明度水印算法（人眼不可见，但在图像处理软件高反差曲线拉升下清晰可辨）。
- **防盗链与防截屏多维防护机制**：
  - 全局拦截右键菜单（Context Menu）、禁止图片拖拽，屏蔽 `Ctrl+S`、`Ctrl+P` 等扒图快捷键。
  - 增加打印介质保护规则（`@media print`），阻断网页直印商用行为。

### 变更 (Changed)
- **成果档案元数据优化**：统一五大核心传世卷（上海矢量肌理、激光剪纸系列、锦江绿道山水剖面等）的比例、门类与原解析度元数据。
- **瓦片网络链路接入**：`assets/config.js` 正式接入 `CangFengCore` 私有云托管切片节点（`https://cangfengcore.lidmwork.workers.dev`）。

---

## [1.0.0] - 2026-09-21

### 新增 (Added)
- **藏锋录初始版本发布 (Initial Release)**：
  - 纯原生前端工程（HTML5 + CSS3 + ES6），完全摒弃冗余的打包器依赖。
  - 集成 OpenSeadragon 5.0，支持 WebP 多层级物理金字塔深览（Deep Zoom）。
  - 支持浅色 / 深色（Light / Dark）艺术馆级主题自由切换。
  - 紧凑网格（Dense）与宽松网格（Comfort）双版式排版。
  - 基于 Canvas 的作品主色调萃取调色板系统（一键复制 HEX 颜色值）。
